apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: dev

images:
  - name: myapp
    newName: myusername/myapp
    newTag: dev

patchesStrategicMerge:
  - replica-count.yaml
  - env-dev.yaml